# Quant Edge Tracker demo media

Current assets (all live in `src/data/projects.ts` demos):

- `dashboard.mp4` / `dashboard.png` — dashboard and bankroll (also the hero)
- `stats.mp4` / `stats.png` — performance and calibration
- `daily-report.mp4` / `daily-report.png` — weekly and daily reports
- `line-shop.mp4` / `line-shop.png` — line shopping and no-vig consensus
- `accounts.mp4` / `accounts.png` — accounts and bankroll ledger

## Ready-to-fill slots (commented out in projects.ts)

- `strategies.mp4` + `strategies.png` — agent strategy execution and runs
  (uncomment once the Strategies page is operational)
- `pipeline.png` — the data pipeline view (uncomment once it ships)

Specs: MP4 H.264, 16:9, 1920x1080 preferred; PNG posters at the same frame.
Optional WebVTT `.vtt` captions per clip via `captionsSrc`.
