# AI News Agent demo media

Current assets (all live in `src/data/projects.ts` demos):

- `hero.png` — panel hero image
- `dashboard.png` — dashboard and briefing archive
- `walkthrough.mp4` / `walkthrough.png` — the agent in action

Candidate additions from the brief (§4.3), if ever produced:

- A generated briefing, top of page (TL;DR, tags, reading time)
- Persistent memory: `[UPDATE]` instead of `[NEW]` tagging

Specs: MP4 H.264, 16:9, 1920x1080 preferred; PNG posters at the same frame.
Optional WebVTT `.vtt` captions per clip via `captionsSrc`.
