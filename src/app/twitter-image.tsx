// Same card as the Open Graph image; the twitter-image file convention is what
// makes Next emit an explicit twitter:image tag (brief §6.1).
export { default, size, contentType, alt } from './opengraph-image';
