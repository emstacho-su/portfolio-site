---
phase: 02-single-page-portfolio-redesign
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package-lock.json
  - src/__tests__/setup.ts
  - src/__tests__/lib/redirects.test.ts
  - src/__tests__/data/projects.test.ts
  - src/__tests__/lib/em-dash.test.ts
  - src/__tests__/components/reduced-motion.test.tsx
  - src/__tests__/hooks/use-scrollspy.test.ts
  - src/__tests__/components/project-popout.test.tsx
  - src/__tests__/components/harness.test.tsx
  - src/__tests__/lib/analytics.test.ts
autonomous: true
requirements: [R-31]
must_haves:
  truths:
    - "node_modules is installed; npm run build, npx vitest run, and npm run lint all run (this is the hard prerequisite per RESEARCH.md; nothing else can run until install completes)."
    - "jsdom has IntersectionObserver and HTMLMediaElement play/pause stubs so the new IO/video hooks and the pop-out are testable (D-23 reduced-motion tests depend on this)."
    - "Every new test file from VALIDATION.md Wave 0 Requirements exists as a runnable scaffold (red allowed) so later waves implement against a fixed contract: redirects (R-16/D-04), projects model (R-25/R-26/D-14/D-15), em-dash sweep over BOTH data exports AND copy-bearing section components (R-20/D-22), reduced-motion (R-30/D-23), scrollspy (R-14/D-02), pop-out a11y (R-27/D-16), harness pillars (R-28/D-18)."
    - "The em-dash scaffold asserts no U+2014/U+2013 across src/data/*.ts exports AND the string literals of src/components/sections/*.tsx (at minimum hero.tsx and harness.tsx), so component-copy regressions (e.g. hero.tsx's current em-dashed TAGLINE) are caught, not just data files."
    - "The baseline suite is green BEFORE any source edits begin, establishing the verification floor for D-24."
  artifacts:
    - path: "src/__tests__/setup.ts"
      provides: "matchMedia + IntersectionObserver + HTMLMediaElement.play/pause jsdom stubs"
      contains: "IntersectionObserver"
    - path: "src/__tests__/lib/redirects.test.ts"
      provides: "scaffold asserting nextConfig.redirects() shape (R-16)"
    - path: "src/__tests__/data/projects.test.ts"
      provides: "scaffold validating the new Project model + featured set (R-25, R-26)"
    - path: "src/__tests__/lib/em-dash.test.ts"
      provides: "scaffold enforcing no U+2014/U+2013 over src/data/*.ts exports AND src/components/sections/*.tsx string literals (R-20)"
      contains: "components/sections"
    - path: "src/__tests__/components/reduced-motion.test.tsx"
      provides: "scaffold + mockMatchMedia helper for R-30"
    - path: "src/__tests__/hooks/use-scrollspy.test.ts"
      provides: "scaffold mocking IntersectionObserver for R-14"
    - path: "src/__tests__/components/project-popout.test.tsx"
      provides: "scaffold for dialog open/ESC/focus-return (R-27)"
    - path: "src/__tests__/components/harness.test.tsx"
      provides: "scaffold asserting six pillars (R-28)"
  key_links:
    - from: "src/__tests__/setup.ts"
      to: "vitest.config.ts setupFiles"
      via: "existing setup reference (already wired)"
      pattern: "IntersectionObserver"
---

<objective>
Install dependencies and lay down the full Wave 0 test scaffold so every later wave executes against a fixed, runnable verification contract.

Purpose: `node_modules` is absent from this working tree (verified in RESEARCH.md Environment Availability). No build, test, lint, or dev command can run until `npm install` completes, so this is the single hardest execution gate. The same install run confirms the baseline is green before any source edits, and the test scaffolds give downstream waves their acceptance targets (Nyquist: tests-first).

Output: installed `node_modules` + `package-lock.json`; extended jsdom stubs in `setup.ts`; eight new/extended test files (red-but-runnable is acceptable for the scaffolds that target not-yet-written code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REDESIGN-SPEC.md
@.planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md
@.planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md
@.planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md

<interfaces>
<!-- Current analytics enum the section_view test will extend (src/lib/analytics/types.ts) -->
eventTypes = ['page_view','project_click','resume_download','contact_click'] as const
analyticsEventSchema = z.object({ event_type: z.enum(eventTypes), event_target: z.string().optional(), session_id: z.string().min(1), ... })

<!-- Current jsdom stub in src/__tests__/setup.ts: only window.matchMedia is mocked (matches:false). -->
<!-- Template for the matchMedia RTL helper lives in src/__tests__/components/ArchitectureTab.test.tsx:6-20 (copy BEFORE that file is deleted in Wave 2). -->
<!-- em-dash sweep scope: src/data/*.ts exports (read each module, walk string values) AND src/components/sections/*.tsx (read each file's SOURCE TEXT and assert no U+2014/U+2013 in string literals; reading file text via fs is simplest since component string literals are not all exported). hero.tsx's current TAGLINE contains an em dash and MUST fail this scaffold until 02-03 retargets it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and confirm a green baseline</name>
  <files>package-lock.json</files>
  <read_first>
    - package.json (read the scripts block and the declared versions; nothing is added or removed here)
    - .planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md (Environment Availability + "Installation (Wave 0, mandatory)": node_modules absent, node v24.15.0, npm 11.12.1)
    - AGENTS.md (after install, confirm whether node_modules/next/dist/docs/ exists; RESEARCH.md notes it is NOT shipped in the tarball, so the AGENTS.md path is satisfied via Context7 pinned to next 16.2.2 instead)
  </read_first>
  <action>
    Run `npm install` at the repo root to populate node_modules (none present) and produce package-lock.json. Do NOT add, remove, or upgrade any dependency: every library the phase needs (gsap, @gsap/react, lenis, motion, @base-ui/react, @supabase/ssr, zod, vitest) is already declared per RESEARCH.md "Standard Stack". After install, run `npm run build`, `npx vitest run`, and `npm run lint` to capture a baseline. The existing suite MUST be green at this point (it is the verification floor for D-24). If any baseline command fails for a reason unrelated to the absent install, stop and report rather than editing source. Confirm the published Base UI import scope is `@base-ui/react/dialog` now that node_modules exists (resolves RESEARCH.md assumption A2); record the confirmed path in the SUMMARY for Wave 3.
  </action>
  <verify>
    <automated>npm run build; npx vitest run; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/` exists and `package-lock.json` is present.
    - `npm run build` exits 0.
    - `npx vitest run` exits 0 (baseline suite green) BEFORE any other task in this plan modifies a test file, OR (if scaffolds are added first) the only failures are the intentionally-red scaffolds from Tasks 2-3 that target not-yet-written code.
    - `npm run lint` exits 0.
    - SUMMARY records the confirmed Base UI dialog import path (A2).
  </acceptance_criteria>
  <done>Dependencies installed; baseline build/test/lint captured green; Base UI import scope confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Extend jsdom stubs in setup.ts</name>
  <files>src/__tests__/setup.ts</files>
  <read_first>
    - src/__tests__/setup.ts (current matchMedia stub via Object.defineProperty(window, ...))
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (T-1: add IntersectionObserver + HTMLMediaElement.play/pause using the same Object.defineProperty idiom)
    - .planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md (Wave 0 Gaps: jsdom lacks IntersectionObserver and media play/pause)
  </read_first>
  <action>
    Add a global `IntersectionObserver` class stub to `src/__tests__/setup.ts` exposing `observe`, `unobserve`, `disconnect`, and `takeRecords` no-ops and storing the callback so tests can drive entries manually (use-scrollspy and section-view hooks depend on this). Add `HTMLMediaElement.prototype.play` (returning a resolved Promise) and `HTMLMediaElement.prototype.pause` (no-op) stubs since jsdom omits both and the demo-video hook calls `.play().catch(...)`. Keep the existing `matchMedia` stub unchanged. Use the existing `Object.defineProperty(window, ...)` idiom for consistency.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib src/__tests__/data</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/setup.ts` defines a global `IntersectionObserver` with observe/unobserve/disconnect/takeRecords.
    - `HTMLMediaElement.prototype.play` and `.pause` are defined (play resolves a Promise).
    - The existing fast deterministic suites still run without "IntersectionObserver is not defined" errors.
  </acceptance_criteria>
  <done>jsdom exposes IntersectionObserver and media play/pause; existing fast suites unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold all new Wave 0 test files and extend the analytics test</name>
  <files>src/__tests__/lib/redirects.test.ts, src/__tests__/data/projects.test.ts, src/__tests__/lib/em-dash.test.ts, src/__tests__/components/reduced-motion.test.tsx, src/__tests__/hooks/use-scrollspy.test.ts, src/__tests__/components/project-popout.test.tsx, src/__tests__/components/harness.test.tsx, src/__tests__/lib/analytics.test.ts</files>
  <read_first>
    - src/__tests__/lib/analytics.test.ts (T-2 template: import the module, iterate the enum, expect over values; extend it to include 'section_view' and assert legacy events still parse)
    - src/__tests__/components/ArchitectureTab.test.tsx (T-3 template: copy the mockMatchMedia(reducedMotion) helper VERBATIM into reduced-motion/project-popout/use-scrollspy/harness tests BEFORE this file is deleted in Wave 2)
    - .planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map: exact filenames and the requirement each asserts)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (T-2, T-3 idioms)
    - .planning/REDESIGN-SPEC.md (§4.3 project data model the projects.test asserts; §4.4 six pillars the harness.test asserts)
    - src/components/sections/hero.tsx (its current TAGLINE contains an em dash; the broadened em-dash scaffold must cover this file so the regression is caught)
  </read_first>
  <action>
    Create the eight test files from VALIDATION.md "Wave 0 Requirements", each anchored to its requirement. (1) `redirects.test.ts` (R-16): import `nextConfig.redirects()` from next.config.ts and assert it resolves to six entries with the six legacy sources (`/projects`, `/projects/:slug*`, `/resume`, `/harness`, `/interested`, `/toolkit`) and `/?s=<section>` destinations; assert the chosen permanent status. (2) `data/projects.test.ts` (R-25, R-26, D-14, D-15): assert every project has id/slug/title/hook/overview/tech/status/links/heroImage and an ordered `demos[]` whose entries have a valid `type` of 'video'|'image', and that the featured set ids are exactly Quant Edge Tracker, AI News Agent, EV Trainer. (3) `lib/em-dash.test.ts` (R-20, D-22): assert no U+2014 (—) or U+2013 (–) appears in (a) any string export from src/data/*.ts AND (b) the source text of src/components/sections/*.tsx (at minimum hero.tsx and harness.tsx) - read the component files' text (e.g. via fs.readFileSync over a glob of src/components/sections/*.tsx) and assert neither codepoint appears in any string literal. Note hero.tsx's current TAGLINE contains an em dash, so this scaffold will be RED until 02-03 retargets the TAGLINE (D-08); that is the intended red. (4) `components/reduced-motion.test.tsx` (R-30, D-23): paste mockMatchMedia and stub the assertions for components built in Waves 3-4 (allowed to be `it.todo` or skipped until those components exist). (5) `hooks/use-scrollspy.test.ts` (R-14, D-02): drive the mocked IntersectionObserver entries and assert the active id (target the not-yet-written hook; red allowed). (6) `components/project-popout.test.tsx` (R-27, D-16): assert dialog opens on trigger, ESC closes, onOpenChange fires, focus returns to trigger (target not-yet-written component; red allowed). (7) `components/harness.test.tsx` (R-28, D-18): assert the six pillar headlines render (target not-yet-written section; red allowed). (8) Extend `lib/analytics.test.ts` (R-17, D-05): add a case proving `analyticsEventSchema` accepts `section_view` once the enum is extended in Wave 2 (mark `.todo`/skipped now so the baseline stays green, or assert the current enum and leave a clearly-labeled pending case). Scaffolds targeting code that does not yet exist may be RED; do NOT make them pass by stubbing production code here. Mark intentional reds with descriptive `it.todo`/`describe.skip` where keeping the baseline green matters for Task 1's green-baseline assertion. The em-dash component-copy assertion over hero.tsx is an intended red until 02-03 fixes the TAGLINE; label it clearly so 02-03 knows it is its acceptance target.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib src/__tests__/data 2>&1; npx vitest list</automated>
  </verify>
  <acceptance_criteria>
    - All eight files exist at the exact paths in files_modified.
    - `npx vitest list` enumerates each new test/suite (files are syntactically valid and discovered).
    - `em-dash.test.ts` asserts over BOTH src/data/*.ts exports AND the source text of src/components/sections/*.tsx (at minimum hero.tsx and harness.tsx); the hero.tsx TAGLINE em-dash case is present (and may be intentionally red until 02-03).
    - The mockMatchMedia helper is present in reduced-motion.test.tsx, project-popout.test.tsx, use-scrollspy.test.ts, and harness.test.tsx (copied from ArchitectureTab.test.tsx before its deletion).
    - Any failing scaffold fails for "component/hook/config not yet implemented" or the intended em-dash regression, not for a syntax/import-resolution error.
    - analytics.test.ts still passes (the section_view case is pending/skipped until Wave 2).
  </acceptance_criteria>
  <done>Eight Wave 0 test files exist and are discoverable; the em-dash scaffold covers data exports AND section-component copy; the matchMedia helper is preserved; baseline-green assertion in Task 1 holds (intended reds clearly labeled).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Third-party package contents enter the build at install time. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm install / dependency tree | accept | No version changes are made; `npm install` resolves the already-declared, pinned-by-caret versions and writes package-lock.json. No new packages added (RESEARCH.md "Nothing new needs to be installed"). Lockfile commit makes the resolved tree auditable. |
| T-02-02 | Tampering | test scaffolds | accept | Test files run only under vitest in jsdom; no network or filesystem writes beyond the test runner (fs reads are read-only over the repo's own source). No attack surface. |
</threat_model>

<verification>
- `npm run build`, `npx vitest run`, `npm run lint` all run (install succeeded).
- Baseline suite green before source edits (D-24 floor), with intended reds clearly labeled.
- jsdom exposes IntersectionObserver + media play/pause.
- Eight Wave 0 test files discoverable via `npx vitest list`.
- em-dash scaffold covers data exports AND section-component copy (hero.tsx + harness.tsx at minimum).
</verification>

<success_criteria>
- node_modules installed; package-lock.json committed.
- setup.ts extended with IO + media stubs.
- All eight Wave 0 test files created/extended and discoverable.
- em-dash.test.ts guards both src/data/*.ts and src/components/sections/*.tsx copy.
- Base UI dialog import path confirmed and recorded for Wave 3 (A2).
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-page-portfolio-redesign/02-01-SUMMARY.md` recording: the confirmed Base UI dialog import path (A2), whether node_modules/next/dist/docs/ exists, the em-dash scaffold's coverage (data + section components), and which scaffolds are intentionally red (including the hero.tsx TAGLINE em-dash case that 02-03 must fix).
</output>
