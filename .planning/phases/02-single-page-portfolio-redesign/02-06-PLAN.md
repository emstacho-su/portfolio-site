---
phase: 02-single-page-portfolio-redesign
plan: 06
type: execute
wave: 4
depends_on: [03, 05]
files_modified:
  - src/components/sections/hero.tsx
  - src/components/ui/interactive-thermodynamic-grid.tsx
  - src/components/fx/hero-grid.tsx
  - src/__tests__/components/reduced-motion.test.tsx
autonomous: true
requirements: [R-18, R-19, R-30]
must_haves:
  truths:
    - "D-06: the thermodynamic hero grid spans the full viewport width via a CSS breakout (absolute inset-y-0 left-1/2 -translate-x-1/2 w-screen), not clipped to the 1200px content container; an overflow-x-hidden ancestor prevents a horizontal scrollbar (no CLS)."
    - "D-07: the grid heat (red squares) clears promptly when the cursor stops (cool to clear at idle, no lingering trail at rest) and re-ignites when the cursor moves again, tuned in the CANVAS component interactive-thermodynamic-grid.tsx (NOT the unused fx/hero-grid.tsx)."
    - "D-23: every new animation across the phase (hero grid, project panels, pop-out, demo video, and any scrollspy-adjacent motion) short-circuits under prefers-reduced-motion; a reduced-motion RTL test asserts animated components render their final state with no inline transition under matchMedia reduce."
    - "The unused duplicate src/components/fx/hero-grid.tsx is deleted (the hero renders the ui/ canvas version; the DOM grid is dead code)."
  artifacts:
    - path: "src/components/sections/hero.tsx"
      provides: "full-bleed grid breakout wrapper"
      contains: "w-screen"
    - path: "src/components/ui/interactive-thermodynamic-grid.tsx"
      provides: "idle-clear heat tuning (prompt cool at rest, re-ignite on move)"
    - path: "src/__tests__/components/reduced-motion.test.tsx"
      provides: "R-30 reduced-motion short-circuit assertions across new animations"
  key_links:
    - from: "src/components/sections/hero.tsx"
      to: "src/components/ui/interactive-thermodynamic-grid.tsx"
      via: "full-bleed wrapper around the canvas grid"
      pattern: "w-screen"
    - from: "src/__tests__/components/reduced-motion.test.tsx"
      to: "the new animated components"
      via: "matchMedia reduce -> final-state assertion"
      pattern: "prefers-reduced-motion"
---

<objective>
Make the hero grid full-bleed with prompt idle-clear heat, delete the dead duplicate grid, and lock in the reduced-motion guarantee with a test that audits every new animation.

Purpose: this is the polish wave. The hero grid changes (D-06/D-07) and the cross-cutting reduced-motion audit (D-23/R-30) depend on Waves 3 and 2 being in place so the test can assert the real animated components (panels, pop-out, demo video, grid) render their final state under `matchMedia` reduce. It is intentionally last before the verification gate.

Output: full-bleed grid wrapper in `hero.tsx`; idle-clear tuning in the canvas grid; deleted `fx/hero-grid.tsx`; finalized `reduced-motion.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REDESIGN-SPEC.md
@.planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md
@.planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md
@.planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md

<interfaces>
<!-- hero.tsx CURRENT: the grid is inside max-w-[1200px] mx-auto (lines ~64,150-161) which CLIPS it. Keep the compile sequence (~75-251) + bootReady gate (~27-38). -->
<!-- RESEARCH Pattern 6 (full-bleed): <div aria-hidden className="absolute inset-y-0 left-1/2 -translate-x-1/2 w-screen overflow-hidden pointer-events-none"><ThermodynamicGrid .../></div>. Use w-screen (not 100vw) for scrollbar gutter; require an overflow-x-hidden ancestor (body/html already behave under Lenis root). The grid's ResizeObserver re-measures automatically. -->
<!-- interactive-thermodynamic-grid.tsx: the LIVE canvas grid (hero uses this). Idle-clear = tune coolingFactor / add idle-decay so heat clears promptly when the pointer stops (RESEARCH Q8). matchMedia reduced-motion guard already at ~line 66. -->
<!-- fx/hero-grid.tsx: UNUSED DOM-cell duplicate; delete (State of the Art / Metadata note). -->
<!-- T-3 reduced-motion test (ArchitectureTab.test.tsx:124-138 pattern): mockMatchMedia(true) -> component renders final state, no inline transition style. Helper already copied in Wave 0. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-bleed hero grid + idle-clear heat tuning; delete the dead duplicate</name>
  <files>src/components/sections/hero.tsx, src/components/ui/interactive-thermodynamic-grid.tsx, src/components/fx/hero-grid.tsx</files>
  <read_first>
    - src/components/sections/hero.tsx (grid currently inside max-w-[1200px] mx-auto:~64,150-161; keep the compile sequence + bootReady gate)
    - src/components/ui/interactive-thermodynamic-grid.tsx (canvas rAF loop, coolingFactor, the matchMedia reduced-motion guard ~line 66, the ResizeObserver ~233-251)
    - .planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md (Pattern 6 full-bleed breakout + the w-screen/overflow-x-hidden CLS note; Q8/D-07 idle-clear tuning; Pitfall 4 single reduced-motion source per code path)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (hero.tsx assignment; the Metadata note that idle-clear goes in the canvas ui/ version, and fx/hero-grid.tsx is the unused duplicate to delete)
    - src/app/globals.css (confirm an overflow-x-hidden / body behavior so w-screen does not introduce a horizontal scrollbar)
  </read_first>
  <action>
    In `hero.tsx`, wrap the thermodynamic grid in the full-bleed breakout (RESEARCH Pattern 6): an `aria-hidden`, `pointer-events-none` div with `absolute inset-y-0 left-1/2 -translate-x-1/2 w-screen overflow-hidden`, so the grid spans the full viewport width while the hero text stays in the 1200px column (D-06). Confirm an ancestor (`body`/`html` or a hero wrapper) has `overflow-x: hidden` so `w-screen` does not add a horizontal scrollbar (CLS/Lighthouse); add it if missing. Keep the compile sequence and `bootReady` gate. In `src/components/ui/interactive-thermodynamic-grid.tsx`, tune the heat decay so red squares clear promptly when the pointer stops: lower the cooling persistence / add an idle-decay path so heat cools to clear at rest with no lingering trail, and re-ignites on movement (D-07/Q8); keep the existing `matchMedia('(prefers-reduced-motion: reduce)')` guard as the single reduced-motion source for this canvas path (Pitfall 4). Delete the unused `src/components/fx/hero-grid.tsx` (the hero renders the canvas `ui/` version; the DOM grid is dead code) and remove any imports of it.
  </action>
  <verify>
    <automated>npm run build; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - hero.tsx wraps the grid in a `w-screen` `left-1/2 -translate-x-1/2` breakout; the hero text column stays at max-w-[1200px].
    - An overflow-x-hidden ancestor exists (no horizontal scrollbar introduced by w-screen).
    - interactive-thermodynamic-grid.tsx idle-decay/cooling is tuned so heat clears at rest; the reduced-motion matchMedia guard is intact.
    - src/components/fx/hero-grid.tsx is deleted with no dangling imports.
    - `npm run build` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The grid is full-bleed with prompt idle-clear heat; the dead DOM grid is removed; build and lint are clean. (Visual confirmation of heat-clear timing is the Wave 5 manual gate.)</done>
</task>

<task type="auto">
  <name>Task 2: Reduced-motion audit + test across all new animations</name>
  <files>src/__tests__/components/reduced-motion.test.tsx</files>
  <read_first>
    - src/__tests__/components/reduced-motion.test.tsx (Wave 0 scaffold with the copied mockMatchMedia helper)
    - src/__tests__/components/ArchitectureTab.test.tsx is deleted; use its preserved mockMatchMedia + final-state assertion pattern (124-138) recorded in Wave 0
    - src/components/projects/project-panel.tsx, src/components/projects/demo-section.tsx, src/components/sections/about.tsx (the animated components to assert short-circuit under reduce)
    - .planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md (Pitfall 4 reduced-motion sources; R-30 mapping)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (S-3 reduced-motion idioms; T-3 test template)
    - .planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md (R-30 row: animated components render final state, no inline transition, under matchMedia reduce)
  </read_first>
  <action>
    Finalize `src/__tests__/components/reduced-motion.test.tsx` to audit R-30/D-23 across the phase's new animations. Using `mockMatchMedia(true)`, render the animated components that have a React-side reduced-motion gate (at minimum project-panel and demo-section, plus an existing gated component such as about's SlideBlock as a regression anchor) and assert each renders its final, visible state with no inline `transition`/animation style applied (mirror the ArchitectureTab final-state assertion). For the video demo, assert that under reduce the `<video>` has no autoplay-driving observer wired and the poster is shown (src omitted). Keep the canvas grid out of this RTL test (its guard is verified by code review / manual since jsdom has no canvas rAF); note that exclusion in the test file. Ensure the test passes with both `mockMatchMedia(true)` and `mockMatchMedia(false)` branches where it makes sense (final-state under reduce; animation hooks present under no-reduce without asserting timing).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/reduced-motion.test.tsx; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `reduced-motion.test.tsx` passes: animated components render final state with no inline transition under matchMedia reduce.
    - The video demo asserts poster-only / no autoplay observer under reduce.
    - The full `npx vitest run` suite is green.
  </acceptance_criteria>
  <done>R-30 is locked in by an automated reduced-motion audit over the new animated components; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pointer events -> canvas grid | client-only pointer-driven rAF; aria-hidden, no data crosses a trust boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Denial of Service (client) | full-bleed canvas grid rAF | accept | The grid already runs a bounded pointer-driven rAF with a reduced-motion kill path; full-bleed widens the canvas but does not change the loop cost class. Verified at the Lighthouse perf gate (Wave 5). |
| T-02-15 | (layout integrity) | w-screen breakout | mitigate | overflow-x-hidden ancestor prevents a horizontal scrollbar / CLS regression. |
</threat_model>

<verification>
- reduced-motion.test.tsx green; full `npx vitest run` green.
- `npm run build` and `npm run lint` clean.
- Manual (Wave 5): grid spans full viewport width and heat clears promptly at rest.
</verification>

<success_criteria>
- Full-bleed grid breakout with no horizontal scrollbar.
- Idle-clear heat tuned (prompt cool at rest, re-ignite on move).
- Dead fx/hero-grid.tsx deleted.
- Reduced-motion audit test green across the new animations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-page-portfolio-redesign/02-06-SUMMARY.md` recording the cooling/idle-decay parameters chosen for the grid and which components the reduced-motion test covers (and which are manual-only, i.e. the canvas grid).
</output>
