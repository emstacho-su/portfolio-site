---
phase: 02-single-page-portfolio-redesign
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/data/projects.ts
  - src/data/about.ts
  - src/data/resume.ts
  - src/components/sections/hero.tsx
  - src/components/sections/about.tsx
  - src/components/sections/resume.tsx
  - src/components/resume/resume-content.tsx
  - src/__tests__/data/projects.test.ts
  - src/__tests__/lib/em-dash.test.ts
autonomous: true
requirements: [R-20, R-21, R-21a, R-21b, R-24, R-25, R-26, R-29]
must_haves:
  truths:
    - "D-08: hero TAGLINE = 'I build AI systems with a ground-truth understanding of how real operations work' and META = \"SYRACUSE IMT '27 / DATA SCIENCE INTERN / AI ENGINEERING\" (verbatim; employer generic)."
    - "D-09: src/data/about.ts replaces BOTH aboutParagraphs and landingAbout with the APPROVED five-paragraph narrative from REDESIGN-SPEC §4.2 verbatim; about.tsx indexing is adjusted from 3 to 5 paragraphs."
    - "D-10: the about section surfaces the pull quote 'The question is never whether I can build it. It is what to build next.'"
    - "D-14: the featured set in src/data/projects.ts becomes Quant Edge Tracker, AI News Agent, EV Trainer; the prior set (GTO Poker, Algo Trading, SchoolworkTrack) is retired."
    - "D-15: the Project model gains id, slug, title, hook, overview, tech, status ('shipped'|'in-progress'), links {repo?,live?}, heroImage, and an ordered demos array of DemoSection {type:'video'|'image', src, poster?, caption, body} (REDESIGN-SPEC §4.3)."
    - "D-13: demo media uses swappable placeholder src/poster paths replaceable with real assets with no code change (R-24)."
    - "D-17: Quant Edge Tracker and EV Trainer soften the gambling angle, framed around statistics, data analytics, modeling, and game theory (not betting/poker)."
    - "D-19: src/data/resume.ts reflects the current Data Science Intern role and rebuilds the projects subsection to match the featured set with softened framing; resume.tsx keeps the PDF download."
    - "D-20: employer names are kept generic on the public on-page render; specific names + phone live only on the downloadable PDF (no phone anywhere in these files)."
    - "D-22: no em dashes (U+2014) or en dashes (U+2013) appear in any src/data/*.ts export; existing resume dateRange en-dashes are replaced."
  artifacts:
    - path: "src/data/projects.ts"
      provides: "extended Project + DemoSection model and the three featured projects"
      contains: "DemoSection"
    - path: "src/data/about.ts"
      provides: "verbatim five-paragraph about narrative + pull quote"
    - path: "src/components/sections/hero.tsx"
      provides: "retargeted TAGLINE/META + #projects CTA"
    - path: "src/data/resume.ts"
      provides: "Data Science Intern role + featured-set projects subsection, generic employer on page"
  key_links:
    - from: "src/components/sections/about.tsx"
      to: "src/data/about.ts"
      via: "landingAbout.paragraphs (5) + pullQuote"
      pattern: "landingAbout"
    - from: "src/components/sections/resume.tsx"
      to: "src/data/resume.ts"
      via: "resumeData projects subsection"
      pattern: "resumeData"
    - from: "src/__tests__/data/projects.test.ts"
      to: "src/data/projects.ts"
      via: "model + featured-set assertion"
      pattern: "Quant Edge"
---

<objective>
Rewrite the site's content layer: the extended project data model + featured set, the approved verbatim about narrative, the retargeted hero copy, and the inline resume rebuilt to the featured set with generic employers. Enforce the no-em-dash convention over all data exports.

Purpose: this is the deterministic, testable half of Wave 2 (content & data). It owns every `src/data/*.ts` file plus the copy-bearing section components, so it also owns the em-dash sweep over those data exports. It is independent of the harness/analytics plan (02-04) and can run in parallel.

Output: rewritten `projects.ts` (model + three projects), `about.ts` (five paragraphs + pull quote), updated `hero.tsx`, `resume.ts`, `resume-content.tsx`, `about.tsx` (5-paragraph indexing); green `projects.test.ts` and `em-dash.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REDESIGN-SPEC.md
@.planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md
@.planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md
@.planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md

<interfaces>
<!-- TARGET model (REDESIGN-SPEC §4.3) -->
interface DemoSection { type: 'video'|'image'; src: string; poster?: string; caption: string; body: string; }
interface Project { id: string; slug: string; title: string; hook: string; overview: string; tech: string[]; status: 'shipped'|'in-progress'; links: { repo?: string; live?: string }; heroImage: string; demos: DemoSection[]; }

<!-- about.tsx CURRENT: consumes landingAbout.paragraphs + landingAbout.pullQuote; renders .slice(0,2) then [2] (3-paragraph layout). MUST adjust to render all 5 paragraphs. Keep SlideBlock motion (has reduced-motion gate). -->
<!-- hero.tsx:10-11 CURRENT constants to replace:
  TAGLINE = 'From ISO audits to AI agents — I build the systems in between.'  (has an em dash; remove)
  META = "SYRACUSE IMT '27 / ISO 9001 AUDITOR / AI-ENGINEERING"
  Hero CTA currently links to /projects (line ~240) -> change to #projects / lenis.scrollTo. -->
<!-- resume.ts CURRENT: dateRange strings use en-dash "–" (lines ~48,57,68); projects subsection lines ~76-102. ResumeData interface ~26-34. -->
<!-- Per-project framings (REDESIGN-SPEC §4.3):
  Quant Edge Tracker: sports analytics + edge modeling; React 19 + TS + Vite, Tailwind v4, shadcn/ui, Supabase, Recharts, Vercel. Lead with statistics/data pipelines/modeling, not betting.
  AI News Agent: autonomous daily briefing; FastAPI, Claude API, Resend, persistent topic memory, Fly.io; later Claude Code Routines + Resend MCP.
  EV Trainer: game-theory decision-modeling trainer; expected-value + decision-tree, range/equity, voice-enabled; TS + React + Supabase. Lead with applied game theory/statistics/decision science, not poker. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the project data model and featured set</name>
  <files>src/data/projects.ts, src/__tests__/data/projects.test.ts</files>
  <behavior>
    - Every project object has: id, slug, title, hook, overview, tech[], status in {'shipped','in-progress'}, links{repo?,live?}, heroImage, demos[].
    - Every demos[] entry has type in {'video','image'}, src, caption, body (poster optional).
    - The three project ids/titles are exactly Quant Edge Tracker, AI News Agent, EV Trainer; the prior set is absent.
    - No demos[] entry omits a required field; the demos array is ordered.
    - No string contains U+2014 or U+2013.
  </behavior>
  <read_first>
    - src/data/projects.ts (current Project interface id/title/subtitle/description/detailedDescription/technologies/status/githubUrl/imageLabel + array literal style)
    - .planning/REDESIGN-SPEC.md (§4.3 exact DemoSection/Project shape and the three per-project framings; §2 softened-framing + no-em-dash conventions)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (projects.ts assignment: current-vs-target interface, D-14/D-15/D-17, S-7)
    - src/__tests__/data/projects.test.ts (the Wave 0 scaffold to turn green)
    - src/components/projects/placeholder-image.tsx (the placeholder media frame heroImage/poster slots map to; informs swappable src/poster paths per D-13)
  </read_first>
  <action>
    Replace the `Project` interface and add a `DemoSection` interface in `src/data/projects.ts` exactly per REDESIGN-SPEC §4.3 (DemoSection: type 'video'|'image', src, poster?, caption, body; Project: id, slug, title, hook, overview, tech[], status 'shipped'|'in-progress', links {repo?, live?}, heroImage, demos[]). Replace the exported array with three projects: Quant Edge Tracker (lead with statistics, data pipelines, closing-line value, edge modeling; tech React 19 + TypeScript + Vite, Tailwind v4, shadcn/ui, Supabase, Recharts, Vercel), AI News Agent (autonomous daily briefing; FastAPI, Claude API, Resend, persistent topic memory, Fly.io; note later Claude Code Routines + Resend MCP), and EV Trainer (game-theory decision-modeling trainer; expected-value + decision-tree, range/equity, voice-enabled; TS + React + Supabase). Use swappable placeholder paths for `heroImage`, demo `src`, and demo `poster` (D-13) so real assets drop in later with no code change. Soften the gambling angle on Quant Edge and EV Trainer toward statistics/analytics/modeling/game theory (D-17). Write your own per-project `hook` and `overview` within that framing (Claude's Discretion per CONTEXT.md). No em dashes (D-22). Update `projects.test.ts` to assert the model shape, valid demo `type` values, ordered demos, and the exact three featured ids.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/data/projects.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/data/projects.test.ts` passes.
    - projects.ts exports `DemoSection` and `Project` matching REDESIGN-SPEC §4.3 field names.
    - The three featured titles are present; GTO Poker / Algo Trading / SchoolworkTrack are absent.
    - No project or demo copy contains "betting" or "poker" as the lead framing for Quant Edge / EV Trainer.
  </acceptance_criteria>
  <done>The extended model and three featured projects exist with swappable placeholder media and softened framing; the model test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Approved about narrative, retargeted hero copy, and the em-dash sweep over data</name>
  <files>src/data/about.ts, src/components/sections/about.tsx, src/components/sections/hero.tsx, src/__tests__/lib/em-dash.test.ts</files>
  <read_first>
    - .planning/REDESIGN-SPEC.md (§4.2 the APPROVED five-paragraph about copy VERBATIM + the pull quote; §4.1 hero tagline/meta; §2 no-em-dash)
    - src/data/about.ts (current aboutParagraphs + landingAbout shape + skillCategories; replace both per D-09/D-10)
    - src/components/sections/about.tsx (current 3-paragraph indexing .slice(0,2)/[2]; adjust to render all 5; keep SlideBlock reduced-motion gate, S-3)
    - src/components/sections/hero.tsx (TAGLINE/META constants:10-11 to replace per D-08; CTA links to /projects -> change to #projects / lenis.scrollTo; remove the em dash in the current tagline)
    - src/__tests__/lib/em-dash.test.ts (the Wave 0 scaffold to turn green over src/data/*.ts exports)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (about.ts/about.tsx/hero.tsx assignments; S-7 em-dash; Pitfall 6 covers both U+2014 and U+2013)
  </read_first>
  <action>
    Replace both `aboutParagraphs` and `landingAbout` in `src/data/about.ts` with the APPROVED narrative from REDESIGN-SPEC §4.2 VERBATIM (five paragraphs; do not paraphrase or invent), and set the pull quote to "The question is never whether I can build it. It is what to build next." (D-10). In `about.tsx`, adjust the paragraph indexing (currently `.slice(0,2)` then `[2]`, a 3-paragraph layout) to render all five paragraphs while keeping the `SlideBlock` scroll motion and its reduced-motion gate. In `hero.tsx`, replace `TAGLINE` with "I build AI systems with a ground-truth understanding of how real operations work" and `META` with "SYRACUSE IMT '27 / DATA SCIENCE INTERN / AI ENGINEERING" (D-08, verbatim, employer generic), and change the hero CTA from a `/projects` route link to a `#projects` anchor that calls `lenis.scrollTo('#projects', { offset: -64 })`. Verify no em/en dashes remain in any copy edited here. Finalize `em-dash.test.ts` to import every export from `src/data/*.ts` and assert no U+2014 and no U+2013 appears in any string value (Pitfall 6 covers both).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/em-dash.test.ts; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `em-dash.test.ts` passes: zero U+2014 and zero U+2013 across all `src/data/*.ts` exports (after this task's edits AND Task 1's projects.ts).
    - about.ts `landingAbout.paragraphs` has exactly 5 entries matching REDESIGN-SPEC §4.2 verbatim; pull quote matches D-10.
    - about.tsx renders all 5 paragraphs (no `.slice(0,2)`/`[2]` truncation remains).
    - hero.tsx TAGLINE/META match D-08 verbatim; the CTA targets `#projects` via lenis.scrollTo.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>About renders the verbatim five-paragraph narrative + pull quote; hero copy is retargeted with a #projects CTA; no em/en dashes remain in any data export.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild the inline resume to the featured set with generic employers</name>
  <files>src/data/resume.ts, src/components/sections/resume.tsx, src/components/resume/resume-content.tsx</files>
  <read_first>
    - src/data/resume.ts (current role, projects subsection lines ~76-102, ResumeData interface ~26-34, dateRange en-dashes ~48/57/68)
    - src/components/sections/resume.tsx (inline render + PDF download button ~48-60; ResumeSectionClient wrapper accepts onResumeDownload)
    - src/components/resume/resume-content.tsx (maps resumeData; projects loop ~41-51)
    - .planning/REDESIGN-SPEC.md (§4.5 inline resume: Data Science Intern role, generic employer on page, rebuilt projects subsection to featured set, retain PDF, no em dashes; §2 phone only on PDF)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (resume.tsx/resume-content.tsx/resume.ts assignments; D-19/D-20)
  </read_first>
  <action>
    Update `src/data/resume.ts` so the current role is Data Science Intern and rebuild the `projects[]` subsection to match the featured set (Quant Edge Tracker, AI News Agent, EV Trainer) with the softened statistics/analytics/game-theory framing (D-19). Keep employer names generic in the on-page-rendered fields ("a manufacturing company" / "manufacturing operations") per D-20; do NOT put a phone number anywhere in these files (phone lives only on the downloadable `/resume.pdf` asset, which is out of scope to regenerate). Replace the en-dash `dateRange` separators with a plain hyphen or "to" (D-22 / Pitfall 6). Keep the `ResumeData` interface shape and the `resume-content.tsx` projects loop; only the data changes. Confirm `resume.tsx` still renders inline with the working PDF download link and the onResumeDownload analytics prop intact.
  </action>
  <verify>
    <automated>npx vitest run; npm run build; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - resume.ts current role reads "Data Science Intern"; the projects subsection lists Quant Edge Tracker, AI News Agent, EV Trainer.
    - No specific employer name and no phone number appear in resume.ts on-page-rendered fields (generic only).
    - No U+2014/U+2013 in resume.ts (covered by em-dash.test.ts from Task 2).
    - resume.tsx retains the PDF download link and onResumeDownload prop.
    - `npx vitest run`, `npm run build`, `npm run lint` all exit 0.
  </acceptance_criteria>
  <done>The inline resume reflects the current role and the featured-set projects with generic employers and no phone; PDF download retained; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| static data files -> rendered DOM | Build-time content only; no runtime user input enters these files. |
| external project links -> new tab | repo/live links open externally. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information disclosure | resume/about copy (privacy) | mitigate | Employer names kept generic on-page and no phone number in any of these files (D-20/D-21); specifics confined to the out-of-scope PDF asset. |
| T-02-07 | (link safety) | external repo/live links rendered from projects.ts | mitigate | Any external anchor rendered from these links must use `target="_blank"` + `rel="noopener noreferrer"` (S-6); the rendering components (Wave 3 panel/popout) enforce it. Data files store URLs only. |
</threat_model>

<verification>
- projects.test.ts and em-dash.test.ts green.
- Full `npx vitest run` green; `npm run build` and `npm run lint` clean (wave-merge gate).
- Content reviewed against REDESIGN-SPEC §4.2 (verbatim about) and §4.3 (project framings).
</verification>

<success_criteria>
- Extended Project/DemoSection model + three featured projects with swappable placeholders and softened framing.
- Verbatim five-paragraph about + pull quote; about.tsx renders all 5.
- Retargeted hero TAGLINE/META + #projects CTA.
- Inline resume with current role, featured-set projects, generic employers, no phone, PDF retained.
- Zero em/en dashes across all data exports.
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-page-portfolio-redesign/02-03-SUMMARY.md` recording the placeholder media paths chosen (for Wave 3 demo-section wiring) and the per-project hook/overview wording authored.
</output>
