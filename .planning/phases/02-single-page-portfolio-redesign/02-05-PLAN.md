---
phase: 02-single-page-portfolio-redesign
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - src/components/sections/projects.tsx
  - src/app/page.tsx
  - src/components/projects/project-panel.tsx
  - src/components/projects/project-popout.tsx
  - src/components/projects/demo-section.tsx
  - src/hooks/use-in-view-video.ts
  - src/components/projects/project-card.tsx
  - src/__tests__/components/project-popout.test.tsx
autonomous: true
requirements: [R-22, R-23, R-24, R-27]
must_haves:
  truths:
    - "D-11: projects render as vertically stacked full-viewport (min-h-screen) overview panels with a GSAP ScrollTrigger scroll-driven entrance animation, and each panel is a scroll destination."
    - "D-12: clicking a panel opens a full-screen, scrollable pop-out case study composed of ordered demo sections from project.demos; each demo section is a scroll destination that plays a short muted clip on scroll-into-view or shows a screenshot plus text."
    - "D-13: demo media uses the swappable placeholder src/poster paths from projects.ts; real assets drop in later with no code change."
    - "D-16: the pop-out is accessible via Base UI Dialog: focus trap while open, ESC to close, background scroll lock, focus return to the trigger on close, and a reduced-motion fallback that shows the poster/screenshot with no autoplay (NOT hand-rolled; do not copy mobile-menu which lacks a focus trap)."
    - "Lenis is paused on pop-out open (lenis.stop()) and resumed on close (lenis.start()); the Dialog Viewport is the native scroll container, so demo-section ScrollTriggers set scroller to the Viewport; ScrollTrigger.refresh() runs on boot and on dialog open (Pitfall 3/5)."
    - "D-23: panel entrances and demo autoplay short-circuit under prefers-reduced-motion (useReducedMotion in components; no autoplay under reduce, poster shown)."
    - "Scroll-into-view video uses muted + playsInline + preload='none' and always .catch() the play() promise (autoplay-policy safe, Lighthouse-budget safe per RESEARCH Pattern 5)."
  artifacts:
    - path: "src/components/sections/projects.tsx"
      provides: "stacked ProjectPanel list + one shared ProjectPopout"
      contains: "ProjectPanel"
    - path: "src/components/projects/project-panel.tsx"
      provides: "full-viewport panel with useGSAP ScrollTrigger entrance"
      contains: "useGSAP"
    - path: "src/components/projects/project-popout.tsx"
      provides: "Base UI Dialog case study + Lenis stop/start"
      contains: "Dialog"
    - path: "src/components/projects/demo-section.tsx"
      provides: "video/image demo block with IO autoplay + poster fallback"
    - path: "src/hooks/use-in-view-video.ts"
      provides: "muted autoplay/pause IntersectionObserver hook"
      contains: "IntersectionObserver"
  key_links:
    - from: "src/components/sections/projects.tsx"
      to: "src/components/projects/project-popout.tsx"
      via: "lifted open/activeProject state, one Dialog"
      pattern: "onOpenChange"
    - from: "src/components/projects/project-popout.tsx"
      to: "lenis"
      via: "lenis.stop() on open / lenis.start() on close"
      pattern: "lenis\\.(stop|start)"
    - from: "src/components/projects/demo-section.tsx"
      to: "src/hooks/use-in-view-video.ts"
      via: "scroller = Dialog Viewport element"
      pattern: "scroller"
    - from: "src/app/page.tsx"
      to: "src/components/sections/projects.tsx"
      via: "real ProjectsSection replaces the Wave 1 inline stub"
      pattern: "ProjectsSection"
---

<objective>
Build the projects centerpiece: stacked full-viewport panels with GSAP scroll-driven entrances, and an accessible full-screen pop-out case study composed of scroll-driven demo sections with muted autoplay video and poster fallbacks; wire the real projects section into page.tsx.

Purpose: this is the highest-risk wave. It depends on the Wave 1 motion plumbing (Lenis on the shared GSAP ticker, the projects anchor stub), the Wave 2 data model (projects.ts demos array), and the Wave 2 harness wiring (02-04 also edited page.tsx; this plan edits page.tsx in a later wave so the changes serialize). The accessibility surface (R-27) is delivered by Base UI Dialog rather than hand-rolled, and all motion routes through the reduced-motion gate.

Output: rewritten `projects.tsx` wired into page.tsx; new `project-panel.tsx`, `project-popout.tsx`, `demo-section.tsx`, `use-in-view-video.ts`; deleted/repurposed `project-card.tsx`; green `project-popout.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REDESIGN-SPEC.md
@.planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md
@.planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md
@.planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md
@src/data/projects.ts

<interfaces>
<!-- Project/DemoSection model from Wave 2 (src/data/projects.ts):
  DemoSection { type:'video'|'image'; src; poster?; caption; body }
  Project { id; slug; title; hook; overview; tech[]; status; links{repo?,live?}; heroImage; demos[] } -->

<!-- page.tsx (after Wave 2) renders the real harness section and a projects inline stub; this plan replaces the projects stub with the real <ProjectsSection/> import. Leave all other sections untouched. -->

<!-- RESEARCH Pattern 2 (panel entrance): useGSAP(() => { if (reduce) return; gsap.from(panel.querySelectorAll('[data-animate]'), { y:60, autoAlpha:0, stagger:0.08, ease:'power3.out', scrollTrigger:{ trigger:panel, start:'top 70%', end:'top 30%', toggleActions:'play none none reverse' } }); }, { scope: panel, dependencies:[reduce] }). registerPlugin(ScrollTrigger, useGSAP). -->

<!-- RESEARCH Pattern 3 (Base UI Dialog): import { Dialog } from '@base-ui/react/dialog' (CONFIRM exact path from Wave 0 SUMMARY / A2). Anatomy: Dialog.Root open/onOpenChange > Portal > Backdrop (fixed inset-0) > Viewport (fixed inset-0 overflow-y-auto = scroll container) > Popup (min-h-screen) > Title (sr-only) + Close (aria-label) + demos.map(DemoSection). Base UI provides focus trap/ESC/scroll-lock/focus-return. -->

<!-- RESEARCH Pattern 5 (scroll-into-view video): video muted playsInline loop preload="none"; IO root = scroller (Dialog Viewport) threshold 0.5; v.play().catch(()=>{}); pause on exit; reduce -> poster only, no observer, src undefined. -->

<!-- Pitfall 3: ScrollTrigger.refresh() after bootReady + document.fonts.ready and after dialog open (layout settles). -->
<!-- Pitfall 5: lenis.stop() on open, lenis.start() on close (Base UI locks body; pausing Lenis prevents fighting). -->

<!-- Analogs: project-card.tsx:50-90 (title/Badge/tech-tag markup); placeholder-image.tsx:12-44 (aspect-video frame + in-view scan-line) for the image demo + poster frame; about.tsx SlideBlock for reduced-motion gate idiom. -->
<!-- S-5: projects.tsx keeps the onProjectClick analytics prop; S-6: external repo/live links use target=_blank rel=noopener noreferrer. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-viewport project panels + the projects section rewrite, wired into page.tsx</name>
  <files>src/components/projects/project-panel.tsx, src/components/sections/projects.tsx, src/app/page.tsx, src/components/projects/project-card.tsx</files>
  <read_first>
    - src/components/sections/projects.tsx (current heading + animated rule:21-33 to KEEP; staggerContainer card grid:35-47 to REPLACE; onProjectClick analytics prop, S-5)
    - src/app/page.tsx (the projects inline stub from Wave 1; replace with the real <ProjectsSection/> import; leave hero/about/harness/resume/contact untouched)
    - src/components/projects/project-card.tsx (title/subtitle/Badge/tech-tag markup:50-90 to adapt; DELETE or repurpose after)
    - .planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md (Pattern 2 useGSAP scoped entrance; Pitfall 3 ScrollTrigger.refresh after boot + fonts)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (project-panel.tsx + projects.tsx assignments; S-3 reduced-motion; S-6 external links)
    - src/data/projects.ts (the demos/heroImage/tech/links/hook/overview fields the panel renders)
    - src/components/projects/placeholder-image.tsx (heroImage placeholder frame)
  </read_first>
  <action>
    Create `src/components/projects/project-panel.tsx`: a `'use client'` full-viewport (`min-h-screen`) overview panel rendering one project's title, `hook`, a large visual (`heroImage` via PlaceholderImage), `overview` line, `tech` Badges (adapt project-card markup), an external links affordance (`target="_blank"` + `rel="noopener noreferrer"`, S-6), and an "open case study" trigger that calls an `onOpen(project)` prop. Animate `[data-animate]` children with `useGSAP({ scope: panel, dependencies: [reduce] })` + ScrollTrigger (RESEARCH Pattern 2); when `useReducedMotion()` is true, return early so content stays in its final state (S-3, D-23). Call `ScrollTrigger.refresh()` after `bootReady` and `document.fonts.ready` (Pitfall 3). Rewrite `src/components/sections/projects.tsx` to keep the heading + animated rule, replace the card grid with a vertical stack of `<ProjectPanel>` (one per project from projects.ts), lift `open`/`activeProject` state here, render a single shared `<ProjectPopout>` (built in Task 2), wire each panel's `onOpen` to set activeProject + open, and keep the `onProjectClick` analytics prop firing on open (S-5). In `src/app/page.tsx`, replace the projects inline stub (from Wave 1) with an import and render of `<ProjectsSection/>` in its scroll-order slot; do not touch the other sections. Delete `src/components/projects/project-card.tsx` (or repurpose if a sub-part is reused) and remove any remaining imports of it.
  </action>
  <verify>
    <automated>npm run build; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - project-panel.tsx exists, is `min-h-screen`, uses `useGSAP` with a `reduce` dependency, and returns early under reduced motion.
    - projects.tsx renders one ProjectPanel per project and a single ProjectPopout with lifted open/activeProject state.
    - page.tsx renders the real `<ProjectsSection/>` (not an inline stub) in its scroll slot.
    - External links use `rel="noopener noreferrer"`.
    - project-card.tsx is deleted or has no dangling imports.
    - `npm run build` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Three stacked full-viewport panels animate on scroll (gated by reduced motion), are wired into the page, and open a shared pop-out; the old card grid is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Accessible Base UI Dialog pop-out with Lenis pause</name>
  <files>src/components/projects/project-popout.tsx, src/__tests__/components/project-popout.test.tsx</files>
  <read_first>
    - .planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md (Pattern 3 Base UI Dialog anatomy; Pitfall 5 lenis.stop/start; Pitfall 3 ScrollTrigger.refresh on dialog open; A2 import path)
    - .planning/phases/02-single-page-portfolio-redesign/02-01-SUMMARY.md (the CONFIRMED @base-ui/react/dialog import path recorded in Wave 0)
    - src/components/navigation/mobile-menu.tsx (body scroll-lock idiom ONLY; do NOT copy as the modal pattern - it lacks a focus trap and would fail R-27)
    - src/app/providers/lenis-provider.tsx (how to obtain the lenis instance to call stop()/start())
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (project-popout.tsx assignment; No-Analog-Found note)
    - src/__tests__/components/project-popout.test.tsx (Wave 0 scaffold to turn green: open on trigger, ESC closes, onOpenChange fires, focus returns)
  </read_first>
  <action>
    Create `src/components/projects/project-popout.tsx`: a single controlled `Dialog.Root` (`@base-ui/react/dialog`, exact path per the Wave 0 SUMMARY) taking `open`, `project`, and `onOpenChange`. Render `Dialog.Portal > Dialog.Backdrop (fixed inset-0) > Dialog.Viewport (fixed inset-0 overflow-y-auto, the scroll container) > Dialog.Popup (min-h-screen w-full)` with an sr-only `Dialog.Title` ({project.title}) and a `Dialog.Close` with `aria-label="Close case study"`. Map `project.demos` to `<DemoSection>` (built in Task 3), passing the Dialog Viewport element as the `scroller` so in-dialog ScrollTriggers/IO measure against the native scroll container (Pattern 3/5). On `onOpenChange(true)` call `lenis.stop()` and `ScrollTrigger.refresh()` (after layout settles); on `false` call `lenis.start()` (Pitfall 5/3). Do NOT hand-roll focus trap/ESC/scroll-lock/focus-return; Base UI provides them (R-27/D-16). Finalize `project-popout.test.tsx` to assert: the dialog opens when `open` is true (or on trigger), ESC fires `onOpenChange(false)`, and focus returns to the trigger on close (mirror the ArchitectureTab fireEvent.keyDown Escape + waitFor flow).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/project-popout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `project-popout.test.tsx` passes: dialog opens, ESC closes via onOpenChange, focus returns to trigger.
    - project-popout.tsx imports Dialog from the confirmed `@base-ui/react/dialog` path and uses Root/Portal/Backdrop/Viewport/Popup/Title/Close.
    - lenis.stop() on open and lenis.start() on close are present; ScrollTrigger.refresh() runs on open.
    - No hand-rolled focus-trap/scroll-lock code; mobile-menu pattern is not copied.
  </acceptance_criteria>
  <done>The pop-out is a Base UI Dialog with native focus trap/ESC/scroll-lock/focus-return, Lenis paused while open; the a11y test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Demo sections with scroll-into-view muted video and poster fallback</name>
  <files>src/components/projects/demo-section.tsx, src/hooks/use-in-view-video.ts</files>
  <read_first>
    - .planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md (Pattern 5 scroll-into-view video: muted/playsInline/preload=none, IO root=scroller, .catch() play(), reduce -> poster only)
    - src/components/projects/placeholder-image.tsx (12-44 aspect-video frame + in-view scan-line for the image demo + poster still)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (demo-section.tsx + use-in-view-video.ts assignments; S-3 reduced motion; S-4 IO cleanup)
    - src/data/projects.ts (DemoSection.type/src/poster/caption/body the block renders)
  </read_first>
  <action>
    Create `src/hooks/use-in-view-video.ts`: an IntersectionObserver hook (S-4 cleanup) that plays a muted `<video>` on intersect and pauses on exit, accepting an optional `scroller` (the Dialog Viewport) as the IO `root` and `threshold: 0.5`. Always `.catch()` the `play()` promise (autoplay may be blocked; poster stays). When `useReducedMotion()` is true, skip the observer entirely and leave the poster showing (S-3, D-23). Create `src/components/projects/demo-section.tsx`: a data-driven block where `demo.type === 'video'` renders `<video muted playsInline loop preload="none" poster={demo.poster} src={reduce ? undefined : demo.src}>` wired to `use-in-view-video` (passing the `scroller` prop through from the pop-out), and `demo.type === 'image'` renders the screenshot via the PlaceholderImage `aspect-video` frame. Each demo section is its own scroll destination rendering `caption` + `body`. Under reduced motion or when `poster` is set before play, show the still with no autoplay (D-16/D-13). Keep swappable placeholder paths so real media drops in with no code change (D-13).
  </action>
  <verify>
    <automated>npx vitest run; npm run build; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - use-in-view-video.ts uses IntersectionObserver with a configurable root (scroller), calls play().catch(), pauses on exit, and disconnects on cleanup; no observer under reduced motion.
    - demo-section.tsx renders `<video muted playsInline preload="none">` for type video and a poster/screenshot for type image; src is omitted under reduced motion.
    - The scroller prop threads from project-popout -> demo-section -> use-in-view-video.
    - `npx vitest run`, `npm run build`, `npm run lint` all exit 0.
  </acceptance_criteria>
  <done>Demo sections autoplay muted on scroll-into-view inside the pop-out, pause out of view, and fall back to the poster/screenshot under reduced motion or blocked autoplay; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| projects.ts links -> rendered external anchors | repo/live URLs open in a new tab. |
| placeholder media src -> <video>/<img> | static placeholder asset paths from data; no user input. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | (link safety / reverse tabnabbing) | external repo/live links in panel/popout | mitigate | All external anchors use `target="_blank"` + `rel="noopener noreferrer"` (S-6). |
| T-02-12 | Tampering | demo media src/poster | accept | Sources are static placeholder paths from projects.ts (build-time); no user input reaches the media element. |
| T-02-13 | Denial of Service (client) | autoplay video / ScrollTrigger count | accept | `preload="none"` + muted single-clip autoplay keeps the budget bounded; only the in-view clip plays. Measured at the Lighthouse gate (Wave 5). |
</threat_model>

<verification>
- project-popout.test.tsx green (a11y wiring).
- Full `npx vitest run` green; `npm run build` and `npm run lint` clean.
- Manual (Wave 5 preview): panels animate on scroll, pop-out opens/closes accessibly, demo video autoplays/pauses, poster fallback under reduced motion.
</verification>

<success_criteria>
- Stacked full-viewport panels with GSAP entrances, each a scroll destination, wired into the page.
- Accessible Base UI Dialog pop-out (focus trap/ESC/scroll-lock/focus-return) with Lenis paused while open.
- Ordered demo sections with muted scroll-into-view autoplay, pause out of view, and poster/screenshot fallback.
- Swappable placeholder media; reduced-motion short-circuit throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-page-portfolio-redesign/02-05-SUMMARY.md` recording how the Dialog Viewport element is passed as the ScrollTrigger/IO scroller and where ScrollTrigger.refresh() is invoked (boot, fonts, dialog open).
</output>
