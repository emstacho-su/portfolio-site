---
phase: 02-single-page-portfolio-redesign
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/data/harness.ts
  - src/components/sections/harness.tsx
  - src/app/page.tsx
  - src/lib/analytics/types.ts
  - src/hooks/use-analytics.ts
  - src/hooks/use-section-view.ts
  - src/app/api/analytics/route.ts
  - src/__tests__/components/harness.test.tsx
  - src/__tests__/lib/analytics.test.ts
  - src/__tests__/components/HarnessTabs.test.tsx
  - src/__tests__/components/ArchitectureTab.test.tsx
autonomous: true
requirements: [R-17, R-28]
must_haves:
  truths:
    - "D-18: the harness section is distilled to six capability pillar cards (Second Brain as RAG, GSD Workflow, Multi-Agent Research, Sub-Agent Execution, Context Engineering, Guardrails), each a short headline plus one to two sentences from REDESIGN-SPEC §4.4; the tabbed data-dense UI and the harness.ts data dump are removed/trimmed drastically."
    - "D-05: src/lib/analytics/types.ts gains 'section_view' in the eventTypes tuple; use-analytics.ts exposes trackSectionView; the /api/analytics route still passes event_type/event_target through with no regression; legacy events still validate."
    - "A new use-section-view hook fires a section_view analytics event once per section on first IntersectionObserver intersection (event_target = section id), reusing the fire-once ref-guard idiom; threshold/shape is Claude's Discretion as long as the /api/analytics Zod contract is preserved."
    - "The two harness-tab tests (HarnessTabs.test.tsx, ArchitectureTab.test.tsx) are deleted because they import removed modules and WILL break; the mockMatchMedia helper was preserved in Wave 0 before deletion (D-24 keeps the suite green)."
    - "D-23: any entrance animation on the harness pillar grid short-circuits under prefers-reduced-motion (uses the existing staggerContainer reveal which already honors the reduced-motion gate)."
    - "The Supabase page_events.event_type column constraint is verified to accept 'section_view' before relying on it (RESEARCH open question A1); if constrained, a migration task is added; if free text, no DB change (input remains Zod-validated and length-bounded)."
  artifacts:
    - path: "src/components/sections/harness.tsx"
      provides: "six-pillar harness section using <Section id=harness>"
      contains: "harness"
    - path: "src/lib/analytics/types.ts"
      provides: "eventTypes tuple including section_view"
      contains: "section_view"
    - path: "src/hooks/use-section-view.ts"
      provides: "fire-once-per-section IntersectionObserver analytics hook"
      contains: "IntersectionObserver"
    - path: "src/hooks/use-analytics.ts"
      provides: "trackSectionView callback"
      contains: "trackSectionView"
  key_links:
    - from: "src/hooks/use-section-view.ts"
      to: "src/hooks/use-analytics.ts"
      via: "trackSectionView(sectionId)"
      pattern: "trackSectionView"
    - from: "src/hooks/use-analytics.ts"
      to: "/api/analytics"
      via: "sendEvent('section_view', sectionId)"
      pattern: "section_view"
    - from: "src/components/sections/harness.tsx"
      to: "src/data/harness.ts"
      via: "six pillar entries (or inlined)"
      pattern: "Second Brain as RAG"
    - from: "src/app/page.tsx"
      to: "src/components/sections/harness.tsx"
      via: "real HarnessSection replaces the Wave 1 inline stub"
      pattern: "HarnessSection"
---

<objective>
Distill the harness into a six-pillar section and extend analytics to section-view events without regression, deleting the now-broken harness-tab tests, and wire the real harness section into page.tsx.

Purpose: this is one of the two independent halves of Wave 2 (content & data). It touches the harness data/section and the analytics contract; it shares no files with the content plan (02-03), so the two run in parallel. It also closes RESEARCH open question A1 (the Supabase column constraint) before any section_view event is relied upon. It depends on 02-02 (Wave 1) because it replaces the harness inline stub that 02-02 left in page.tsx.

Output: a new `harness.tsx` six-pillar section wired into page.tsx; trimmed `harness.ts`; extended analytics enum + `trackSectionView` + `use-section-view` hook; deleted `HarnessTabs.test.tsx` and `ArchitectureTab.test.tsx`; green `harness.test.tsx` and extended `analytics.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REDESIGN-SPEC.md
@.planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md
@.planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md
@.planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md

<interfaces>
<!-- analytics/types.ts CURRENT -->
eventTypes = ['page_view','project_click','resume_download','contact_click'] as const   // ADD 'section_view'
analyticsEventSchema = z.object({ event_type: z.enum(eventTypes), event_target: z.string().optional(), referrer/user_agent/pathname optional, session_id: z.string().min(1) })

<!-- use-analytics.ts CURRENT: trackProjectClick etc. via sendEvent(type, target); fire-once hasFiredPageView ref guard (lines ~10,17-21). ADD trackSectionView(id) => sendEvent('section_view', id). -->
<!-- api/analytics/route.ts CURRENT: supabase.from('page_events').insert({ event_type, event_target, ... }) (line ~40). No change unless DB column is constrained. -->

<!-- page.tsx (after Wave 1 / 02-02) mounts an inline harness anchor stub <section id="harness">; this plan replaces that stub with the real <HarnessSection/> import. Do NOT touch the projects stub (02-05 owns that). -->

<!-- Six pillars (REDESIGN-SPEC §4.4) - headline + one to two sentences each:
  Second Brain as RAG | GSD Workflow | Multi-Agent Research | Sub-Agent Execution | Context Engineering | Guardrails -->

<!-- Card frame analog (MemoryTab.tsx:127-139): <article className="border border-hairline rounded-md p-6 bg-background"> <h3 font-sans text-lg font-semibold> <p font-mono text-xs text-muted-foreground> -->
<!-- Stagger grid analog (contact.tsx:66-72): motion.div variants={staggerContainer} initial="initial" whileInView="animate" viewport={{once:true}} grid grid-cols-1 sm:grid-cols-3 gap-4 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the six-pillar harness section, wire it into page.tsx, and delete the broken tab tests</name>
  <files>src/components/sections/harness.tsx, src/data/harness.ts, src/app/page.tsx, src/__tests__/components/harness.test.tsx, src/__tests__/components/HarnessTabs.test.tsx, src/__tests__/components/ArchitectureTab.test.tsx</files>
  <read_first>
    - .planning/REDESIGN-SPEC.md (§4.4 the six pillars with their exact one-liners; "engineer the system around the AI" story to land)
    - src/data/harness.ts (current multi-export data dump: layers/inventory/hookEvents/stats; trim drastically to a six-pillar array OR delete and inline)
    - src/app/page.tsx (the harness inline stub left by 02-02; replace it with the real <HarnessSection/> import in scroll order; do NOT alter the projects stub which 02-05 owns)
    - src/components/ui/section.tsx OR src/components/sections/section.tsx (S-2: <Section id="harness"> emits the anchor that matches the nav hash)
    - src/components/sections/contact.tsx (66-72 staggerContainer/staggerItem grid analog; already honors reduced motion, S-3; and the MemoryCard frame markup quoted in <interfaces> since the harness route was deleted in Wave 1)
    - src/__tests__/components/harness.test.tsx (Wave 0 scaffold to turn green: assert the six pillar headlines render)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (harness.tsx assignment; "Tests that WILL break (must delete)")
  </read_first>
  <action>
    Create `src/components/sections/harness.tsx` as a `'use client'` `<Section id="harness">` rendering exactly six pillar cards from REDESIGN-SPEC §4.4: Second Brain as RAG, GSD Workflow, Multi-Agent Research, Sub-Agent Execution, Context Engineering, Guardrails, each a short headline plus the one-to-two-sentence one-liner from the spec. Use the MemoryCard `border border-hairline rounded-md p-6 bg-background` frame and a `staggerContainer`/`staggerItem` grid (`grid-cols-1 sm:grid-cols-3`) mirroring contact.tsx, which already short-circuits under reduced motion (S-3, D-23). Source the pillar data from a trimmed `src/data/harness.ts` (replace the entire layers/inventory/hookEvents/stats dump with a single six-entry pillar array) or inline the six entries in the component and delete harness.ts; either satisfies D-18. No em dashes (S-7). In `src/app/page.tsx`, replace the harness inline anchor stub (left by 02-02) with an import and render of `<HarnessSection/>` in its scroll-order slot; leave the projects stub untouched (02-05 owns it). DELETE `src/__tests__/components/HarnessTabs.test.tsx` and `src/__tests__/components/ArchitectureTab.test.tsx` (they import the removed `@/app/harness/_components/*` and `@/data/harness` `layers`; the mockMatchMedia helper was already copied out in Wave 0). Finalize `harness.test.tsx` to render the section and assert all six pillar headlines are present.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/components/harness.test.tsx; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `harness.test.tsx` passes: all six pillar headlines render.
    - `src/__tests__/components/HarnessTabs.test.tsx` and `ArchitectureTab.test.tsx` no longer exist.
    - `harness.ts` is either a six-entry pillar array or deleted (no layers/inventory/hookEvents/stats remain).
    - harness.tsx uses `<Section id="harness">` and a reduced-motion-honoring stagger grid; no em dashes.
    - page.tsx renders the real `<HarnessSection/>` (not an inline stub) in its scroll slot; the projects stub is untouched.
    - `npm run build` exits 0 (no imports to deleted harness modules).
  </acceptance_criteria>
  <done>The harness reads as six scannable pillars wired into the page; the data dump and tab tests are gone; the harness test is green and the build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend analytics to section_view and add the use-section-view hook</name>
  <files>src/lib/analytics/types.ts, src/hooks/use-analytics.ts, src/hooks/use-section-view.ts, src/app/api/analytics/route.ts, src/__tests__/lib/analytics.test.ts</files>
  <read_first>
    - src/lib/analytics/types.ts (eventTypes tuple:3-8 + analyticsEventSchema:10-17; add 'section_view', keep event_target optional)
    - src/hooks/use-analytics.ts (trackProjectClick pattern:49-54 + fire-once hasFiredPageView ref guard:10,17-21; add trackSectionView mirroring it, export it:67-71)
    - src/app/api/analytics/route.ts (insert path:40-47 - already passes event_type/event_target through; no change unless DB column is constrained)
    - .planning/phases/02-single-page-portfolio-redesign/02-PATTERNS.md (use-section-view assignment + S-4 IO cleanup + S-5 analytics handoff; types.ts/use-analytics.ts/route.ts assignments)
    - .planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md (Open Q1 + assumption A1: verify the Supabase page_events.event_type column accepts section_view; Security Domain V5: keep Zod validation, bound event_target length)
    - src/__tests__/lib/analytics.test.ts (extend: assert section_view parses AND legacy events still parse)
  </read_first>
  <action>
    Add `'section_view'` to the `eventTypes` tuple in `src/lib/analytics/types.ts` (one-line, backward-compatible; `event_target` carries the section id). In `use-analytics.ts`, add and export a `trackSectionView(sectionId: string)` callback that calls the existing send path as `sendEvent('section_view', sectionId)`, mirroring `trackProjectClick`. Create `src/hooks/use-section-view.ts`: an IntersectionObserver hook that fires `trackSectionView(id)` exactly once per section on first intersection (use a `Set`/ref guard mirroring the `hasFiredPageView` idiom; S-4 cleanup; threshold and rootMargin are Claude's Discretion within the preserved /api/analytics contract). In `route.ts`, make NO change unless the Supabase verification (below) shows the column is constrained. As a V5 hardening step, bound `event_target` length in the Zod schema (e.g. a reasonable max) so junk/oversized targets are rejected. Verify the Supabase `page_events.event_type` column with the Supabase MCP (`list_tables` on the analytics project): if it is a constrained Postgres enum/CHECK, add a follow-up migration task in the SUMMARY (and apply it) to allow `section_view`; if it is free text, record "no DB change needed" and rely on the code-only path. Extend `analytics.test.ts` to assert `analyticsEventSchema` accepts `section_view` and still accepts all four legacy event types (no regression), and that an over-length `event_target` is rejected.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lib/analytics.test.ts; npm run build; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `analytics.test.ts` passes: section_view + all four legacy types validate; over-length event_target rejected.
    - types.ts eventTypes includes 'section_view'; event_target has a bounded max length.
    - use-analytics.ts exports `trackSectionView`; use-section-view.ts fires once per section via IntersectionObserver and disconnects on cleanup.
    - SUMMARY records the Supabase column-constraint finding (constrained -> migration applied; free text -> no DB change).
    - `npm run build` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Analytics accepts section_view with no regression; a fire-once section-view hook is wired to trackSectionView; the Supabase column constraint is resolved and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> /api/analytics POST | Untrusted JSON body crosses into the analytics persist path. |
| /api/analytics -> Supabase page_events | Validated event is appended to the table via the SSR client. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering / DoS | /api/analytics event body | mitigate | Zod `analyticsEventSchema.safeParse` rejects malformed bodies (kept); event_target gains a bounded max length so oversized/junk targets are rejected (V5). |
| T-02-09 | Tampering | section_view event_target value | mitigate | event_target is a short section-id string inserted via the parameterized Supabase client (no string concatenation into SQL); never reflected into HTML. |
| T-02-10 | (data integrity) | page_events.event_type DB constraint | mitigate | Column constraint verified before relying on section_view; migration applied only if the column is a constrained enum/CHECK (RESEARCH A1). |
</threat_model>

<verification>
- harness.test.tsx and analytics.test.ts green; the two tab tests deleted.
- Full `npx vitest run` green; `npm run build` and `npm run lint` clean (wave-merge gate).
- Supabase page_events.event_type accepts section_view (verified, with migration applied if needed).
</verification>

<success_criteria>
- Harness reads as six scannable pillars wired into the page; tab UI and data dump removed.
- Analytics accepts section_view with no regression; trackSectionView + use-section-view wired.
- Broken harness-tab tests deleted; suite green.
- Supabase column constraint resolved and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-page-portfolio-redesign/02-04-SUMMARY.md` recording the Supabase column-constraint finding (and any migration applied), the section_view IntersectionObserver threshold chosen, and where the six pillar data lives (trimmed harness.ts vs inlined).
</output>
