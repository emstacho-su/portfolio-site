---
phase: 02-single-page-portfolio-redesign
plan: 07
type: execute
wave: 5
depends_on: [02, 03, 04, 05, 06]
files_modified: []
autonomous: false
requirements: [R-13, R-22, R-23, R-31]
must_haves:
  truths:
    - "D-24: next build, vitest (full suite), and lint pass cleanly; the harness-tab tests are gone and the new pillar/redirect/model/em-dash/scrollspy/popout/reduced-motion tests are green; Lighthouse performance is at least 95 and accessibility is exactly 100 on the Vercel preview."
    - "D-01/D-13/D-12/D-11: the deployed preview is one scrollable page where the three project overview panels animate on scroll and the pop-out opens and closes accessibly with placeholder demo sections (manual QA per VALIDATION.md Manual-Only Verifications)."
    - "D-04/D-05: all six legacy URLs (/projects, /projects/<slug>, /resume, /harness, /interested, /toolkit) 30x to the right section via ?s=, and Supabase is still logging events including section_view (no analytics regression)."
    - "D-06/D-07: on the preview the hero grid is full-bleed width and the heat clears promptly at rest (manual visual gate that cannot run in jsdom)."
  artifacts: []
  key_links:
    - from: "legacy URL request"
      to: "correct on-page section"
      via: "redirect ?s= + client lenis.scrollTo"
      pattern: "\\?s="
    - from: "section intersection"
      to: "Supabase page_events"
      via: "section_view event"
      pattern: "section_view"
---

<objective>
Run the full verification gate for Phase 2: automated build/test/lint, then deploy a Vercel preview and confirm the manual-only behaviors (scroll feel, pop-out UX, full-bleed grid, redirects, analytics, Lighthouse) that cannot be asserted in jsdom.

Purpose: this is the final wave. It proves the phase success criteria (D-24/R-31) and the visual/runtime behaviors that the unit suite cannot reach (GSAP/Lenis feel, canvas heat, real-browser autoplay policy, hash navigation, Lighthouse lab metrics). It gates the phase as done.

Output: a green automated gate, a deployed preview, and a human sign-off on the manual checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REDESIGN-SPEC.md
@.planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md
@.planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gate and Vercel preview deploy</name>
  <files>none (verification + deploy only)</files>
  <read_first>
    - .planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md (Sampling Rate "Before /gsd-verify-work": build clean, full vitest green, lint clean, Lighthouse in preview; Per-Task Verification Map for the full automated command set)
    - .planning/phases/02-single-page-portfolio-redesign/02-RESEARCH.md (Suggested Wave 5 verification list: full vitest+lint+build, preview, Lighthouse, six redirects, Supabase logging incl. section_view)
  </read_first>
  <action>
    Run the automated gate at the repo root: `npm run build`, `npx vitest run` (full suite), and `npm run lint`; all three MUST exit 0. Confirm the test inventory reflects the phase: the new redirects/projects/em-dash/scrollspy/project-popout/harness/reduced-motion tests are present and green, the analytics test includes the section_view case, and `HarnessTabs.test.tsx` + `ArchitectureTab.test.tsx` are gone. Then deploy a Vercel preview for the branch (push the branch and let the Vercel GitHub integration build it, or run the Vercel CLI if authenticated; if a Vercel auth error occurs, surface it as an auth gate for the user rather than failing the plan). Capture the preview URL for the manual checkpoint. Do NOT mark the phase complete on the automated gate alone; the manual checkpoint (Task 2) is blocking.
  </action>
  <verify>
    <automated>npm run build; npx vitest run; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npx vitest run`, `npm run lint` all exit 0.
    - The deleted tab tests are absent; the new phase tests are present and green; analytics test covers section_view.
    - A Vercel preview URL is captured (or an auth gate is surfaced to the user).
  </acceptance_criteria>
  <done>The automated gate is green and a preview URL is available for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual QA, redirects, analytics, and Lighthouse sign-off</name>
  <files>none (human verification on the Vercel preview)</files>
  <what-built>
    A single-page portfolio: anchor-nav + scrollspy, three GSAP scroll-entrance project panels with accessible Base UI Dialog pop-out case studies (placeholder demo media), a six-pillar harness section, inline resume with PDF, full-bleed hero grid with idle-clear heat, six legacy-route redirects to ?s= sections, and section-view analytics. The automated gate (build/test/lint) is already green and a Vercel preview is deployed.
  </what-built>
  <read_first>
    - .planning/phases/02-single-page-portfolio-redesign/02-VALIDATION.md (Manual-Only Verifications table: the exact behaviors and instructions to check)
    - .planning/REDESIGN-SPEC.md (§4.2 verbatim about copy; §4.3 project framings; §4.4 six pillars - for the content/tone review)
  </read_first>
  <action>
    HUMAN CHECKPOINT (no code). Present the Vercel preview URL to the user and walk the nine-point manual checklist below; this is a blocking gate per VALIDATION.md Manual-Only Verifications. Claude facilitates (opens the preview, runs Lighthouse if it has browser tooling, hits each legacy URL and reports the status code, queries Supabase via MCP for section_view rows) but the user gives final sign-off. Any failing item is captured as a gap for `/gsd-plan-phase --gaps`.
  </action>
  <how-to-verify>
    On the Vercel preview URL:
    1. Scroll the whole page top to bottom: confirm Hero -> About -> Projects -> Harness -> Resume -> Contact render in order on one page (D-01/R-13); the nav underline (scrollspy) tracks the active section; clicking each nav link smooth-scrolls to its section.
    2. Projects: confirm the three panels animate on scroll-into-view (R-22); click each panel to open the pop-out; confirm it is full-screen and scrollable, demo sections scroll, a muted clip autoplays on scroll-into-view and pauses out of view, and an image+text demo renders (R-23/R-24).
    3. Pop-out a11y: Tab cycles within the dialog (focus trap), ESC closes it, background does not scroll while open, and focus returns to the panel trigger on close (R-27).
    4. Hero grid: confirm it spans the FULL viewport width (edge to edge), heat (red squares) clears promptly when you stop moving the cursor and re-ignites on movement (R-18/R-19); no horizontal scrollbar.
    5. Reduced motion: enable OS "reduce motion", reload, confirm panels/pop-out/grid/video do not animate or autoplay and posters/screenshots show (R-30).
    6. Redirects: visit each legacy URL on the preview (/projects, /projects/anything, /resume, /harness, /interested, /toolkit) and confirm each lands on the correct section via ?s= (R-16); confirm a permanent (308/301) status.
    7. Analytics: open the network tab, scroll through sections, and confirm section_view POSTs to /api/analytics return 200; confirm in Supabase that rows with event_type='section_view' appear (no regression on existing event types) (R-17).
    8. Content/tone: spot-check that the About copy matches REDESIGN-SPEC §4.2, employers are generic, no phone appears on the public site, and there are no em dashes (R-20/R-21/R-21a/R-21b).
    9. Lighthouse: run Lighthouse (mobile + desktop) on the preview; confirm Performance >= 95 and Accessibility = 100 (R-31).
  </how-to-verify>
  <verify>
    <automated>MISSING — manual checkpoint; no jsdom-runnable assertion. Automated coverage lives in Task 1 (build/test/lint) and the wave 1-4 test suites; the items here (GSAP/Lenis feel, canvas heat, real-browser autoplay, hash navigation, Lighthouse lab metrics) are inherently browser-only per VALIDATION.md Manual-Only Verifications.</automated>
  </verify>
  <acceptance_criteria>
    - One-page scroll order correct; scrollspy + smooth-scroll work; all six anchors reachable.
    - Three panels animate; pop-out opens/closes accessibly (focus trap, ESC, scroll lock, focus return); demo media plays/pauses; image+text demo renders.
    - Hero grid is full-bleed and heat clears at rest; no horizontal scrollbar.
    - Reduced motion short-circuits all new animations (posters shown, no autoplay).
    - All six legacy URLs redirect (permanent) to the right section via ?s=.
    - section_view events log to Supabase with no regression on legacy events.
    - Content matches REDESIGN-SPEC; generic employers; no phone; no em dashes.
    - Lighthouse Performance >= 95 and Accessibility = 100.
  </acceptance_criteria>
  <done>The user has reviewed the preview and signed off on all nine checklist items (or filed gaps for the failing ones).</done>
  <resume-signal>Type "approved" if every item passes, or describe the failing items (each becomes a gap-closure task).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public preview -> /api/analytics | The live preview exercises the only POST endpoint with real traffic. |
| legacy URL -> redirect destination | Inbound legacy links cross the routing layer. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | (verification) | redirect destinations | mitigate | Manual check confirms all six destinations are static internal `/?s=` sections (no open redirect) and return a permanent status. |
| T-02-17 | (verification) | analytics endpoint | mitigate | Preview confirms Zod still rejects malformed bodies and only the bounded section_view/legacy events persist; no PII in the payload. |
</threat_model>

<verification>
- Automated: `npm run build`, `npx vitest run`, `npm run lint` all green.
- Manual checkpoint: all nine preview checks pass; Lighthouse perf >= 95 / a11y = 100.
- Six redirects verified in a real browser; Supabase logging confirmed including section_view.
</verification>

<success_criteria>
- D-24/R-31: build + full vitest + lint clean; Lighthouse perf >= 95, a11y = 100.
- One-page scroll with working anchor-nav + scrollspy; old routes redirect; analytics intact.
- Project panels animate; pop-out opens/closes accessibly; placeholder demo sections work.
- Hero grid full-bleed and clears at rest; tone retargeted; About matches the approved copy; no em dashes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-page-portfolio-redesign/02-07-SUMMARY.md` recording the Lighthouse scores (mobile + desktop), the preview URL, the verified redirect statuses, Supabase section_view confirmation, and any gaps that became follow-up tasks.
</output>
